# orientbench 科学问题与失败教训

审计基线：`ziyu24/orientbench@ea337b2d6df813c5460f1a3db844ce2802b8ba09`

## 项目研究什么

项目研究旋转检测的“方向可靠性”是否被常用框指标掩盖，以及几何方向误差能否预测独立的下游应用风险并支持校准、选择或融合。

## 实际采用过的方法

项目做受控角度扰动、AP50/AP75 与角误差对照，使用图像级 LTT 选择覆盖率，并在 HRSC 等条件上测试应用迁移、风险 selector、融合、CMR、GR-EQS 和有效方向干预。

## 教训一：AP50 可以掩盖严重方向错误

- 失败命题：旋转框在 AP50 基本不变时，方向质量也可视为不变。
- 失败原因：受控角度扰动使 AP50 近乎保持，但 AP75 下降约 0.176 至 0.398，平均角误差增加约 29 至 32 度；宽松重叠阈值没有分辨方向破坏。
- 后续做法：方向敏感任务必须同时报告更高 IoU 阈值、周期角误差和形状分层结果，不能用 AP50 单独验收。
- 边界：该结果确证的是度量盲区；几何方向误差本身还不是独立的下游行动损失。
- 证据：`ziyu24/orientbench@ea337b2d6df813c5460f1a3db844ce2802b8ba09` 的 `lab/result.md`。

## 教训二：几何度量异常不等于下游风险有因果增量

- 失败命题：角误差或方向不可靠性高，必然能在独立应用中预测额外失败并支持风险选择。
- 失败原因：HRSC 上的前瞻应用迁移置信区间为负或跨零，selector、head 和融合路线没有形成独立增量；几何相关性没有自动转化为应用效用。
- 后续做法：在不复用几何真值定义的 held-out 应用上预设损失函数，比较方向信号相对置信度、形状和 IoU 的条件增量。
- 边界：负结果限制当前应用和信号，不否定方向对航向、停靠或姿态任务可能重要。
- 证据：`ziyu24/orientbench@ea337b2d6df813c5460f1a3db844ce2802b8ba09` 的 `lab/failed_methods.md`、`lab/result.md`。

## 教训三：选择规则恒取全覆盖时没有选择性证书

- 失败命题：图像级 LTT 给出覆盖率一就证明模型已学会在可靠样本上选择。
- 失败原因：最优规则恒等于保留全部样本，没有产生拒绝或风险分层；形式覆盖成立但证书是平凡的。
- 后续做法：同时要求覆盖保证和非平凡保留率/效用改善，并与全保留基线直接比较。
- 边界：全覆盖可作为安全默认，但不能宣称选择器获得了区分能力。
- 证据：`ziyu24/orientbench@ea337b2d6df813c5460f1a3db844ce2802b8ba09` 的 `lab/failed_methods.md`、`lab/result.md`。

## 教训四：干预没有改变中介量时不能裁决机制

- 失败命题：若若干图像操作后性能变化很小，就能否定“有效方向信息驱动结果”的机制。
- 失败原因：这些操作没有充分改变预设的有效方向量或中位方向偏差，中介操纵失败；结果只能说明当前干预弱，方法效果仍未知。
- 后续做法：先验证干预确实大幅、定向地改变中介而尽量保持其他因素，再观察结果响应；操纵检查是因果解释的前提。
- 边界：重复、强而特异的有效干预若仍无响应，才可对该机制形成负证据。
- 证据：`ziyu24/orientbench@ea337b2d6df813c5460f1a3db844ce2802b8ba09` 的 `lab/failed_methods.md`、`lab/result.md`。
