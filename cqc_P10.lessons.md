# cqc_P10 科学问题与失败教训

审计基线：`ziyu24/cqc_P10@3c92d3ef13a40c2838ea8ccbd7826807106c06ec`

## 项目研究什么

项目研究能否用样本对训练监督的有符号效用，按模型容量动态选择或路由训练数据，使不同容量的旋转检测器获得不同且有益的监督。

## 实际采用过的方法

项目以目标梯度、监督梯度内积和小校准集近似样本效用，构造硬符号路由，并在多个模型容量、随机种子和独立面板上检查短期 Taylor 预测与长期训练收益。

## 教训一：瓦片存在性不能替代实例级监督效用

- 失败命题：一张瓦片是否含目标，可以作为其中各实例旋转框对训练价值的代理。
- 失败原因：瓦片标签混合了实例数量、类别、难度和背景，一个二元存在变量不能决定具体实例或损失项的梯度贡献。
- 后续做法：把效用定义在实例、匹配或明确损失项上，并用实例级真值检查代理的排序与符号一致性。
- 边界：图像级任务或每图单实例数据中，瓦片存在性可能足够；不能无条件迁移到密集检测。
- 证据：`ziyu24/cqc_P10@3c92d3ef13a40c2838ea8ccbd7826807106c06ec` 的 `lab/failed_methods.md`、`lab/result.md`。

## 教训二：没有目标实例的校准集不能定义目标效用

- 失败命题：任意小校准集上的梯度都能估计目标类别的训练效用。
- 失败原因：部分校准瓦片没有目标实例，所得梯度由背景或其他类别主导，与声称的目标效用不是同一对象。
- 后续做法：先审计目标实例覆盖、匹配数和损失构成；采用分层抽样或明确的目标条件风险。
- 边界：若研究目标就是总体损失而非目标类别效用，背景梯度可以合法纳入，但必须改写主张。
- 证据：`ziyu24/cqc_P10@3c92d3ef13a40c2838ea8ccbd7826807106c06ec` 的 `lab/failed_methods.md`、`lab/result.md`。

## 教训三：近零梯度内积不适合硬符号决策

- 失败命题：梯度内积大于或小于零可以稳定决定保留或丢弃样本。
- 失败原因：大量观测贴近数值零点，舍入、批组成和轻微参数变化即可翻转符号，硬门把测量噪声放大成离散训练差异。
- 后续做法：报告效应大小和不确定区间，设置不可判定区或采用连续权重，并对数值尺度与重复抽样做稳定性分析。
- 边界：远离零且跨批次、跨种子稳定的符号仍可作为证据。
- 证据：`ziyu24/cqc_P10@3c92d3ef13a40c2838ea8ccbd7826807106c06ec` 的 `lab/failed_methods.md`、`lab/discussion.md`。

## 教训四：比较容量效用前各容量必须具有共同能力

- 失败命题：即使极小模型在目标任务上几乎没有能力，也能与正常模型比较监督效用的方向。
- 失败原因：部分容量的关键类别精度为零，效用差混入“是否会做任务”和“怎样利用样本”两种变化，容量交互不可解释。
- 后续做法：先建立各容量的共同非退化基线与相近训练阶段，再比较条件效用；达不到共同能力就缩小容量范围。
- 边界：研究能力涌现本身可以包含退化模型，但估计对象必须改为能力阈值而非样本路由。
- 证据：`ziyu24/cqc_P10@3c92d3ef13a40c2838ea8ccbd7826807106c06ec` 的 `lab/failed_methods.md`、`lab/result.md`。

## 教训五：局部 Taylor 正确性不能外推长期训练收益

- 失败命题：小校准集上的一步梯度预测准确，就足以支持固定路由在完整训练中持续有益。
- 失败原因：单种子方向翻转在多种子后消失，固定小梯度记忆在五个独立面板均未稳定预测总体；参数轨迹改变后局部线性近似失效。
- 后续做法：把一步近似和长期因果效应分开检验，使用多种子、独立校准面板和随轨迹更新的估计，并以最终同预算性能裁决。
- 边界：局部近似仍适合短步诊断或在线更新，不能未经验证声称长期课程学习有效。
- 证据：`ziyu24/cqc_P10@3c92d3ef13a40c2838ea8ccbd7826807106c06ec` 的 `lab/failed_methods.md`、`lab/result.md`、`lab/sug.md`。
