# cqc_P15：检测错误历史与重复训练收益

## 快速阅读路径

先读研究问题与实际方法，再读八条教训及文末停止边界。当前来源为`ziyu24/cqc_P15@04768e648f489fc56d7b36a9f0653e6a106fd6b2`，相对入口：`lab/discussion.md`、`lab/result.md`、`lab/failed_methods.md`。已抓取来源全部远端分支，仅有main，没有更新更晚的次线。前三条保留其原始证据提交入口。

## 项目研究什么

在不增加教师的前提下，检测器的对象错误历史能否在当前难度之外预测“再训练一次”的增量泛化收益，并最终改善计入观测、估计和训练成本的等时间检测精度？研究对象是收益与训练时机，不是单纯重访高loss样本。

## 领域位置与当前结论

历史采样、复习与泛化收益选择已有相关方法，名称或工具组合本身不是创新证据；定位见来源`doc/related_work.md`。普通检测器在当前配置中能够学习：开发集AP50从12轮8.752升至36轮74.897。早期低性能不能代表后续阶段。

固定36轮状态下，已测试的六次IoU统计/趋势加Ridge没有显示额外预测支持，停止扩展该表示。完整错误轨迹、其它估计器和实际等时间调度收益尚未知。有限负结果没有证明一般历史方法或重复训练无效。后续两个训练条件的诊断中，全局收益秩相关接近零，但预设前四分之一交叉选择均有正收益；联合投入参考未通过，局部正线索仍保留。

## 实际采用过的方法

HRSC2016单类检测，固定种子42，训练/开发/描述性留出348/88/181图；R50-FPN Oriented R-CNN，batch8、学习率0.0025。先在早期锚点比较重复与间隔，再恢复未舍入AP并核对普通学习曲线；后续在36轮做32组不重叠八图候选的两臂配对，每臂42更新，共64分支，最后曝光时刻及后续背景更新受控。

预测目标是开发集末端AP50的连续第二次曝光减一次曝光。四折按候选组划分，比较Current与静态、有序、固定打乱旧历史。实际历史为六次连续IoU统计及趋势；另一“整图无预测”通道全零，不是GT漏检轨迹。Current的面积未归一化，单图loss诊断也未实现规定的随机状态恢复，因此不称完整错误历史协议已验证。描述性留出此前已被查看，官方test未使用。

后续复用同一36轮模型/优化器，在原候选中按ID/fold固定16组，每组在两个预定背景/增强条件下做两臂，共64分支、2688更新，仅评估开发集最终端点。同条件所有候选共享背景，背景排除已选候选；同候选两条件固定同设备，基础模型仍为单种子。保存实际事件、终点模型/优化器/RNG及原始预测。此诊断没有拟合新历史表示。

另一条已完成的选择路线是AFSS型图像采样适配：从公开ImageNet初始化训练，共享12轮528次更新后，比较普通随机采样和按训练图像P/R充分度分组、带防遗忘重访的采样。普通基线固定72轮3168次更新；两臂按相同更新数调整学习率，AFSS另保存记录时钟达到基线预算的端点。共42次全训练集评分；实际选图、批次、补齐、增强种子及学习率均按冻结规则重建一致。同更新数下开发集AP50/AP75下降2.3937/6.4059个百分点，当前配置没有显示收益。记录时钟端点同样下降，但部分开销漏计，尚不能声明严格全部成本等时间比较完成。


随后完成普通、随机局部放大和几何状态分配三臂。HRSC单种子，共享36遍历后训练至72遍历，图片流相同、放大配额一致，180/348图有合法裁剪窗口；用训练图当前匹配IoU在0.5至0.75区间内的残差分配视图。6336个训练事件和六次评分后的动作可重建。几何分配的主AP75未超过随机放大，增强正值保留；共同增强随机流未按协议共享，预算内评测用错模型，所以因果和严格成本结论须收窄。

纠正共同随机条件后，又从同一公开初始化共享36遍历，比较普通训练与随机局部放大至72遍历。实际图序、逐图增强键与翻转、模型随机种子和学习率均核对一致；开发集随机放大相对普通的AP50/AP75/AP50:95为-1.0049/+7.2581/+0.7741个百分点，描述性留出为+0.3227/+3.7525/+0.6933。AP75改善与AP50损失同时成立，联合投入参考未通过。新评测显式绑定加载模型，旧预算端点也已按保存状态重新评价；但成本仍为组件求和而非完整连续时钟，不能据此宣称全成本收益。

后续又从保存的同一公共状态完成普通双视图、完整图加局部图和完整图分类/双视图回归三种配方，每种新增1584更新、每步16视图。后两者相对普通对照均提高AP75但降低AP50与平均AP；分类监督移回完整图没有恢复精度。最后一臂1584条事件的实际图序、动作、学习率、模型随机键和翻转均与源局部双视图匹配；四项损失按冻结权重重算一致。

最后以36遍历前六个实际模型对全部训练图逐GT重新推理，按正式AP的置信度、ignore和重复框规则构造遗忘/恢复及关联分数波动；现时图像特征不含旧P/R回填。用两个旧条件的16组收益拟合冻结Ridge，并在其余16个新条件候选上预选后完成32条真实once/consecutive配对。所有分支保存端点模型、预测和.5/.75 PR；未读取测试集。首次配对因内存起点可变而撤回，随后从持久共同前缀独立恢复32次，检查模型/动量/RNG并重做端点评测。其后固定两通道（IoU/关联score）水平—趋势卡尔曼状态，以旧A/B收益标签冻结选择，在新的K14背景生成新的共同前缀并完成16组32条配对；保存重载检查、端点、预测和PR，未用K14结果调参。

## 教训一：历史增量必须相对于同一时点、真实可观测的当前状态解释

- 失败命题：把早期简化特征的阴性预测结果，或已经混入历史的Current对照，解释成“对象错误历史没有增量信息”。
- 失败原因：早期检测器质量低，普通延续随后大幅提高，阶段条件并不等价；原分析还曾使用锚点之后观测。后续Current的IoU又误取整个六次窗口均值，实际对照已经含历史。另一个被称为漏检历史的通道实际上只标记整图无预测，且全零。上述条件均不能支撑对完整对象错误轨迹的广义否定。纠正时点后，Current与静态/有序历史的MSE为3.2185与3.9962/4.2793，仍仅支持实际IoU表示的有限负结果。
- 后续做法：明确当前、历史窗口和预测未来的时间关系，用锚点当前观测构造Current；按保存数据的真实语义命名表示，并在无效通道或未实现协议存在时收窄结论。普通模型的学习正证据与某个收益表示的负结果分别保留。
- 边界：这是比较信息条件与结果归因的修正，不是完整错误轨迹有效的证据。单种子、单类、固定阶段和低维回归结果均不能外推为普遍零效应；也不自动要求重跑或添加复杂估计器。
- 证据：`ziyu24/cqc_P15@e2b0f9442de5a9c33b71ccf9a8e109935fe4de2d`；`lab/result.md`记录分阶段AP、实际字段与无重训修正；`src/analyze_mature_probe.py`和`src/test_analyze_mature_probe.py`对应当前时点与历史隔离；`src/run_mature_probe.py`记录实际历史采集；`doc/mature_repeat_probe.md`给出原协议边界。

## 教训二：候选收益分散和事后正子集不足以支持继续训练调度方法

- 失败命题：只要配对收益有正有负，或预测排序的前四分之一实际均值为正，就足以支持增加历史估计器、扩大重复实验或进入等时间调度。
- 失败原因：32组末端开发集净差均值-0.1764 AP百分点，14组为正，范围[-3.7742,+3.7544]；分散既可能包含真实差异，也可能包含当前有限实验的变动，未被分离。Current的MSE=3.2185，静态/有序历史更高；补充训练折均值参考为2.9421。Current选取子集虽有+0.2552，但没有证明历史增量或上线净收益。原“难例”正收益来自排序方向反转，真正难例子集均值为-0.4299。描述性留出也不足以充当新盲测。后续16组两条件诊断的AP50平均净收益为+0.2920/+0.8662，Spearman=0.0206，描述区间[-0.6342,+0.5692]；但双向交叉选择净收益+1.1277/+1.2934、相对目标条件全组均值的增益+0.2615/+1.0014，均为正。因此整体排序缺少支持与局部选择正线索可以同时成立，不能互相替代。
- 后续做法：同时报告完整配对均值、分布、冻结折预测与无特征参考，把子集描述和独立部署验证分开；在已定预算内完成核心性能信号后，依据缺少增量支持停止扩展已测表示。新方案须提出独立机制依据和可负担的区分性验证，不能靠不断加种子、组数或方法复杂度追求阳性。提前区分全局排序与局部选择目标；未通过联合投入参考时保留每项指标的真实结果，不能事后改阈值，也不能用一个低相关值抹掉预设选择的正线索。
- 边界：停止的是六次IoU统计/趋势加Ridge这一具体投入，属于经验止损，不是证明真实效应为零。无特征参考是事后审计补充；尚未完成在线调度或全部成本下的等时间比较。前次主端点预测保留，辅助端点原预测被覆盖与随机恢复记录不足也限制精确重建声明。后续条件间诊断虽保存了完整事件和终点状态，但相同干预前输入/种子在第二至八步已出现loss差异，最大0.0077613；根因及AP贡献未知，不能把低相关仅归因于背景/增强。两条件、固定开发集和单锚点不足以分离所有变异，反事实选择也不是理论上界或可部署方法。
- 证据：`ziyu24/cqc_P15@e2b0f9442de5a9c33b71ccf9a8e109935fe4de2d`；`lab/result.md`给出完整配对统计、交叉拟合、补充参考及成本/恢复限制；`src/analyze_mature_probe.py`定义收益、折划分、难例方向和参考；`configs/hrsc_mature_probe_manifest.json`保存固定候选；`lab/failed_methods.md`及`lab/discussion.md`限定止损范围；`src/analyze_gain_reproducibility.py`、`src/run_gain_reproducibility.py`和`doc/gain_reproducibility.md`对应新增两条件诊断、实际执行及预先定义的分析。

## 教训三：图像充分度采样迁移必须用固定末端性能和实际观测成本裁决

- 失败命题：把学习充分度分组与防遗忘重访迁移到旋转检测器，就能在固定更新数或实际时间预算内提高精度；或者早期曲线领先足以证明最终收益。
- 失败原因：这次HRSC适配中采样确实执行，42次评分后的选择可由保存统计重建；但固定3168次更新的开发集AP50/AP75从普通随机采样的80.5278/36.5165降至78.1341/30.1106，描述性留出从90.1542/49.2878降至87.7886/36.2450。早期约1个百分点的AP50领先未保持到预定末端。全训练集评分累计约926秒增加观测成本；按已记录时钟到达预算的端点也更差。具体是阈值、训练分布、检测器适配还是其他机制导致，现有对照没有分离，不能编造单一根因。
- 后续做法：先验证实际选择与固定训练日程，再同时保存同更新数和明确成本口径的时间端点；观察者评分、选图、状态恢复和必要写盘均须在宣称全部成本比较前纳入。固定末端负结果支持停止当前配置的扩展，不能通过事后选择早期最高点或连续调整阈值来改写已完成比较。
- 边界：这是HRSC单类、单种子、R50-FPN Oriented R-CNN上的AFSS型适配，不是原方法跨架构复现或所有选择策略的否定。官方test未使用，描述性留出不是新盲测。旧报告误用训练最终总时间描述预算端点，实际记录时钟超出约0.64秒；纠正报告不改变同更新数AP下降。由于选图、恢复及部分写盘未完整计入，不外推严格全成本等时间结论。保存的训练评分仅含P/R统计，不能独立复算原始匹配几何；恢复游标也未完整保存，不能称逐位复现已通过。
- 证据：`ziyu24/cqc_P15@5a253f0b75b08fce22631abafeed89bd832300b9`；`lab/result.md`、`lab/failed_methods.md`保存端点性能和无重训审计边界；`src/afss_sampling.py`、`src/run_afss_training.py`记录实际采样和计时实现；`src/audit_afss_training.py`重建事件与端点时钟；`configs/afss_hrsc.json`和`doc/afss_hrsc_training.md`给出冻结配置与协议。

## 教训四：增强收益、分配收益与预算端点必须分别核验

- 失败命题：某个状态分配臂超过普通训练，就足以证明分配有效；图片顺序和动作配额相同，就足以视为共同随机条件下的干预比较；保存了两个时间状态，就代表两端性能已正确评估。
- 失败原因：HRSC三臂的开发集AP75为普通35.8519、随机放大38.4532、几何分配37.2107。几何分配高于普通却低于随机放大，不能用增强收益替代分配增量；其AP50相对随机仍有+0.4825，主指标与辅助指标不能择优替换。两个放大臂实际配额一致，5.1768%的动作发生改变，但全部对应步的基础增强种子不同，策略与随机轨迹未被冻结协议隔离。预算内外预测又都由预算后模型生成，原记录不能代表预算内成绩。后续已按保存状态修复端点评测，但没有补回缺失时钟，仍不能以旧记录作严格效率否定。
- 后续做法：保留普通训练、同配额随机动作与状态分配三个比较对象；按预定主指标判断额外收益，同时记录辅助正值。要求共享随机流时验证实际种子与变换，不能只看图序。时间端点需绑定实际加载的模型和完整时钟；有保存状态不等于已评价正确状态。优先修复比较定义，不能靠换指标、追加阈值或扩展种子补足错误对照。修正共同增强后，若高IoU指标改善而宽松IoU指标下降，应分别保留并诊断检测变化；联合参考未通过不是所有性能信号都为负。
- 边界：当前几何启发式未形成继续投入依据，但不同随机轨迹限制机制解释；这不是所有几何信息无效的严格否证。随机放大的正值只保留为单种子线索，不称稳定因果增益。预算后几何AP75本身高于普通约0.9824，不能因预定主比较未通过就把成本问题也判成阴性。后续双臂新训练已纠正共同增强，开发集AP75提高7.2581点、AP50下降1.0049点、AP50:95提高0.7741点，不能沿用旧随机流缺陷否定新结果，也不称跨种子稳定。计时仍漏掉部分模型加载、校验和必要写盘；保存加载后下一批一致也不是从持久游标重建的真实中途恢复。旧错误诊断采用IoU排序贪心，不等价于AP的置信度顺序匹配；后续冻结预测已按标准匹配复核，检出转移得到支持，但训练根因尚未确认。
- 证据：`ziyu24/cqc_P15@ca8ce9a6a32fa1fc991f408ba0549d285236d13f`；`lab/result.md`、`lab/failed_methods.md`保存三臂绝对指标、点差及修正；`configs/geometry_action_hrsc.json`为冻结条件；`src/run_geometry_action_training.py`显示实际增强随机流与时间评测；`src/audit_geometry_action.py`重建实际事件、配额与预测文件一致性；`configs/zoom_control_hrsc.json`与`lab/result.md`给出后续共享增强对照、性能权衡及计时和匹配语义限制。

## 教训五：输出反事实与曝光统计只能支持其实际操作范围内的解释

- 失败命题：移除纯背景框后AP差距仍在，就能排除置信度排序；裁剪训练中记录到目标移出，就能将开发集漏检归因为训练目标丢失。
- 失败原因：冻结预测的标准PR复算保留AP75提高7.2581点、AP50下降1.0049点；严格匹配TP净增8，宽松匹配TP净减4。删除与全部GT最大旋转IoU低于0.1的框后，AP50差从−1.0049变为−1.5197，但这没有干预前景排序、重复框或NMS。GT辅助几何修复虽使差值变为+1.4151，也同时改变匹配关系，不能解释为可实现训练增益。另一个曝光字段每逢图片排期就加一，错误命名为完整视图；34539次GT排期实际分为完整视图24656、裁剪保留9013、裁剪移出870。统计单位是事件，T/D目标身份不同，不能用这些事件直接解释D的4个净漏检。
- 后续做法：将每种反事实的改动对象与未改动因素分别列明；置信度排序AP匹配与几何贪心修复使用不同名称，不能互换。曝光按排期、实际完整视图、裁剪保留及移出分别记录，并核对互斥分区合计。若提出保留完整视图的新训练，应设置相同计算量的对照，作为待验证假说，而非诊断已经确认的修复机制。
- 边界：这些结果支持输出层面的定位—检出权衡，不是上下文缺失、排序或训练曝光的唯一因果解释。净增目标数不是一组被逐个改善的相同目标；分组原始数量也不证明富集。单种子HRSC、已暴露的开发/描述集和冻结post-NMS输出均限制外推。后续双视图和损失分配已完成，具体裁决见教训六；质量评分基线尚未验证。
- 证据：`ziyu24/cqc_P15@ca8ce9a6a32fa1fc991f408ba0549d285236d13f`；`lab/result.md`保存标准PR积分、跨阈值转移及修正曝光充分统计；`src/analyze_zoom_tradeoff.py`定义实际匹配、反事实和互斥曝光；`src/audit_zoom_tradeoff.py`复算保存PR并纠正旧字段语义；`src/test_zoom_exposure.py`检验曝光守恒；`configs/zoom_tradeoff_diagnosis.json`保留冻结诊断条件。

## 教训六：完整视图覆盖与分类监督分离不能直接保证消除增强的精度权衡

- 失败命题：保留完整图监督即可避免局部放大的宽松IoU精度损失；若仍下降，把分类监督全部移回完整图就能恢复检出与总体精度。
- 失败原因：匹配16视图/步和新增1584优化步的普通对照，D AP50/AP75/AP50:95为81.6889/33.7503/41.1276；完整图加局部图为79.5525/37.6326/40.5849。将RPN/ROI分类全部分配给完整图、两个视图回归各半后，变为78.3521/36.4250/40.3184，相对前一配方三项均下降。原输入和随机控制已重放，实际损失组合符合固定定义；不能把该结果解释为动作未执行。新分配严格TP为121，比前一配方119更多但AP75更低，说明最终TP数量不能代替整条PR曲线表现。
- 后续做法：给额外视图设置相同前后向次数的普通对照，分别报告严格/宽松IoU与平均AP，不能只保留定位正值。损失分配须核对实际分项权重、输入和随机轨迹；固定配方未恢复目标性能时结束其扩展，而非连续扫描权重、比例或时长。要研究排序，应在框集合保持不变的条件下单独测试可学习分数，先作为基线而非现成创新。
- 边界：这是HRSC单类、单种子、固定检测器与日程下的两次有限干预，不否定其它视图方法或一般分类/定位分离。完整图加入同时改变尺度、曝光和权重，不能唯一归因为上下文缺失；TP与AP关系也不能证明排序是唯一根因。共享增强单视图的AP75及平均AP正值保留。旧成本不全、新计时仍在部分事件写入之前截止，不宣称全成本效率失败。新评分方法尚未完成。
- 证据：`ziyu24/cqc_P15@ca8ce9a6a32fa1fc991f408ba0549d285236d13f`；`lab/result.md`给出三配方固定端点、原始PR积分及1584条重放核对；`src/run_full_view_zoom.py`、`src/run_loss_view_routing.py`为实际训练与损失实现；`src/analyze_r010.py`复算三臂原始预测；`configs/full_view_zoom_hrsc.json`、`configs/loss_view_routing_hrsc.json`为冻结对照；`lab/failed_methods.md`限定停止范围。

## 教训七：干预分支必须恢复同一实际起点，运行完成不能代替因果比较有效

- 失败命题：保存state_dict到内存、每次调用加载函数，就足以保证重复与替换训练从同一模型和优化状态出发，因而可以用终点差否定历史选择。
- 失败原因：实际执行缓存直接引用模型参数的可变存储，第一条分支训练同时改变了所谓共同起点，后续加载该缓存并没有复原。相同PyTorch版本的无训练数据最小复现确认该别名行为。原先公布的历史相对Current选择差−0.1975点因此混入不同起点/额外训练影响，不能作为历史表示失败或停止其研究的证据。真实历史、冻结选择及旧标签拟合改善不修复这一比较缺陷。
- 后续做法：每条干预分支从不可变持久状态独立恢复模型、优化器和随机状态，并核对加载值；用可改变参数与动量的回归案例验证恢复，而非只检查文件存在。纠错保留既定候选、特征、预测器、选中组和成功标准，不利用错误结果调方案。数据全集与加载覆盖独立于预测核对。
- 边界：撤回的仅是错误起点产生的旧负裁决。持久重载是配对有效的必要条件，不能代替对历史表示价值的裁决；后续固定卡尔曼条件的有限负结果见下一条。单种子、单类、16个评价组与条件迁移仍限制外推。
- 证据：`ziyu24/cqc_P15@04768e648f489fc56d7b36a9f0653e6a106fd6b2`；`lab/result.md`、`lab/failed_methods.md`给出冻结门、全量指标及范围；`src/checkpoint_restart.py`、`src/test_checkpoint_restart.py`提供独立恢复与回归检查；`src/run_r012_pairs.py`实施逐分支持久恢复，`src/hrsc_input_audit.py`核对实际数据身份与加载映射，`src/audit_r013_pr.py`核验32条PR积分。

## 教训八：冻结状态估计器须在新的有效配对中同时超过当前信息与随机选择

- 失败命题：给六次对象IoU和关联score历史加入固定卡尔曼水平/趋势状态，就会比锚点Current信息更好地选择值得第二次曝光的候选。
- 失败原因：在不读新条件收益的前提下，以旧A/B标签固定Q=.001、R=.01、Ridge与top4后，K14的Kalman选择AP50净收益为−1.1369点，既低于Current的−0.9905点，也低于16组随机期望−0.6003点；相对Current=−0.1464点、相对随机=−0.5366点且自身为负。评价组MSE/Spearman=4.6528/−.0412，也没有优于Current的4.0504/−.0265。32条分支均从新的持久共同前缀恢复，故该裁决不是上一条的可变起点缺陷。
- 后续做法：为状态估计器预先冻结观测模型、拟合标签、选择数和新条件，再在独立共同前缀上报告相对当前、随机期望和自身绝对收益；三者未达门时停止该固定组合，而非在同一证据上扫描噪声、窗口、回归器、组数或种子寻找阳性。
- 边界：这是HRSC单类、单基础种子、固定六次观测、A/B→K14迁移和16组评价下的经验负结果；不否定对象历史、不同状态模型/观测计划、不同迁移条件或在线及全成本调度。AP75的局部正值不能替换预设AP50主标准。
- 证据：`ziyu24/cqc_P15@04768e648f489fc56d7b36a9f0653e6a106fd6b2`；`lab/result.md`与`lab/failed_methods.md`给出冻结协议、全量端点、门和成本边界；`configs/kalman_gain_transfer.json`、`configs/hrsc_kalman_gain_manifest.json`固定状态与候选；`src/kalman_history.py`、`src/select_kalman_groups.py`和`src/run_r012_pairs.py`实现冻结和独立恢复；`src/audit_r013_pr.py`核验32条PR积分。

## 方法族停止索引

| 方法或主张 | 当前裁决 | 不应外推到 |
|---|---|---|
| 早期缩减历史加轻量回归 | 未显示增量；撤回一般历史无效解释 | 成熟阶段或完整错误轨迹 |
| 36轮六次IoU统计/趋势加Ridge | 本次固定预算后停止扩展 | 所有历史、其它估计器或数据集 |
| 两固定训练条件的候选收益迁移 | 联合投入参考未通过；保留局部选择正线索 | 不等于收益不存在，也未识别变异根因 |
| HRSC上的AFSS型图像充分度与防遗忘采样 | 固定末端精度下降，停止当前配置扩展 | 原AFSS、其他适配和所有选择策略；严格全成本结论仍受计时限制 |
| 固定卡尔曼历史状态（Q=.001、R=.01、Ridge、top4） | 有效K14配对未超过Current或随机，停止该组合扩展 | 所有历史状态模型、观测计划、迁移条件或在线/全成本调度 |
| 普通检测器学习能力 | 保留实测正结果 | 不声称完全收敛或跨种子稳定 |
| 当前几何状态分配普通/放大视图 | 未超随机放大；保留增强线索及随机流限制 | 不等于几何信息无效；严格预算内效率未知 |
| 共享增强后的随机局部放大 | AP75与平均AP改善，AP50下降约1点，联合参考未通过 | 单种子性能权衡不是整体项目失败；全成本收益及根因未知 |
| 完整图保留及分类/回归视图分配 | 两个固定配方均未消除AP50损失，停止扩展 | 不否定全部视图方法；严格TP增加不代表AP必然增加 |
| 真实逐GT遗忘/恢复/分数波动加冻结Ridge选择 | 修复后仍未超过Current，停止当前表示扩展 | 不否定其它历史表示、迁移条件或在线收益 |
