# cqc_P8 科学问题与失败教训

> 使用边界：教训须结合适用条件与原始证据使用，同条件下的有效反证不能忽略，也不能跨条件自动否决新研究；来源不可访问时须注明“未独立核实”，不得将摘要当作已核实的原始证据。

审计基线：`ziyu24/cqc_P8@5dd67246dec926527c4a5d4f7b06347a25925a50`。已抓取来源全部远端分支，未发现提交时间更新的次线。此前独立执行同图ONNX CPU参考与官方匹配/AP聚合；本次复核冻结小图两项对照的40个数组、哈希、连续误差及编译检查器，修正严格容差未过导致的过度否定，只更新本项目。

快速阅读路径：先读“项目研究什么”→“领域位置与当前结论”→七条教训→“方法族停止索引”。

## 项目研究什么

项目先研究面向旋转检测任务与采集条件的模型压缩，后限定为真实低精度执行的数值与决策诊断：量化误差是否经候选选择/旋转NMS放大，能否区别于普通数值失真、导出或构建图变化。完整强PTQ协议尚未完成；局部比较结束不能替代对未分离恢复线索的科学判断。

## 领域位置与当前结论

- **事实**：名为Task-Compile的旧实现是签名哈希奇偶选择两个预设宽度；所谓Generic/Random也为固定结构。其AP50较固定Generic低1.44点、多14k参数是真实局部观测；计时仅调用共同特征提取，旧“端到端略慢/被完整效率支配”的表述撤回。
- **事实**：ACOR代理与实际损伤Spearman约−0.161，尾部筛查不通过；局部bbox-head W4A4仅四倍率fake quant，OBB选择相对HBB恢复为0，不能外推全网络量化。
- **事实**：固定train-only旋转检测器的真实TensorRT浮点AP75=86.212点，标准W8A8及其审计重建均为0；混合捕获图为30.407点，固定浮点最终候选ID后为33.418点。主干、decode/NMS的精度范围以实际引擎与共享后处理为准，不能称全链路全整数。
- **事实**：后续固定95卷积路径/87唯一模块的逐tensor W8A8模拟，原生AP75=86.050，无迁移=0，连续初始化=85.667，有限搜索=85.017；初始化恢复真实存在，但不是旧TensorRT链路恢复。86层采样重构MSE下降，搜索相对初始化AP75差−0.651点，95%区间[−2.610,+1.338]，没有可识别的搜索增量。
- **事实**：冻结连续初始化图将95份权重量化为静态INT8常量后构建成功，真实静态W8A8覆盖已核验，但AP75仅6.484。同一静态ONNX的独立CPU参考为85.387；ONNX减TensorRT为78.903点，配对95%区间[74.199,83.332]。原8图的原始与静态ONNX共72个输出逐元素一致，而CPU ONNX相对TensorRT最大NRMSE为1.271595；差异在decode/NMS之前已存在。
- **事实**：三卷积小图已复现大幅失真；仅把第二卷积权重DQ尺度改接等值独立常量，固定8图CPU端点逐元素保持，实际静态W8A8最大NRMSE由8.769降至0.002547，约缩小3443倍。单ULP辅助项没有额外收益；7/8图仍超1e-5，新图伴随BN融合变化。局部大幅改善成立，严格保真、完整图AP及内部单因果尚未成立。
- **推断**：存在构建/精度组合的数值敏感性，但候选ID干预只再恢复约3.010点，占混合图剩余55.805点损失约5.4%；当前证据不支持将主要损失归于候选/NMS机制。
- **未知**：最新差异优先定位到TensorRT转换/执行路径，但首坏层、融合或内核的单因果归因以及强MSQuant C/D尚未完成；混合图不是可用部署修复。此前将局部比较结束扩大为整个诊断停止的判断已撤回：混合图的恢复来源仍未分离，也未证实原预算耗尽；不能据此证明全部OBB PTQ无效或强PTQ必然失败。

## 实际采用过的方法

旧实验使用固定宽度结构对照、采样条件代理与局部伪量化。后续使用HRSC固定单种子train-only模型、固定训练校准清单，构建真实FP16/标准W8A8网络引擎，统一浮点解码和旋转NMS，比较末端浮点、按原始候选ID交叉干预，以及现有引擎的完整预测与AP。中间输出捕获改变了实际量化/融合选择；未实现的强方法不能由普通熵校准替代。后续在相同训练图上测全网通道迁移的恒等，连续尺度未达容差，二进制尺度零误差；后续固定尺度对照确认关闭卷积TF32许可时连续尺度达到原恒等容差，允许时仍失败；这不是历史逐层核归因，该恒等对照不包含量化。随后固定128训练图完成全卷积逐tensor对称W8A8数值模拟，以每次调用8个固定patch做有限尺度搜索，一次验证181图并保存全部预测及配对区间。它保留BN/SiLU等浮点计算。随后冻结初始化显式QDQ图，经权重常量化完成真实静态INT8构建和181图评测；独立CPU ONNX前向、相同后处理及官方逐图匹配/AP聚合确认约79点后端差距。三组已存原生/模拟/TensorRT预测的十阈值AP复核到1e-6以内。原/静态ONNX的8图输出完全相同，仅证明该已测输入上的转换保持；CPU参考相对GPU模拟也非逐元素等价，不能用AP接近代替逐算子正确性。

## 教训一：名义方法与局部计时不能支持完整效率裁决

- 失败命题：名为任务结构编译的实验及其延迟表，已经证明任务专用压缩被通用剪枝完整支配。
- 失败原因：实际编译规则是哈希奇偶选择预设结构，对照也非强剪枝算法；困难标记误作角度，权重按文件名词典序选择，几何控制与模型选择不符名义协议。延迟只测共同特征提取，没有经过发生改动的检测头，因此不能识别端到端效率差异。旧审计据名称和局部计时作过度否决，应撤回。
- 后续做法：先核对实际干预、对照算法、训练/权重选择和计时调用路径，再做同预算精度—效率比较。明确哪些模块被测；修复实验有效性与形成新方法贡献分开。
- 边界：较差AP和参数差仍约束该固定实现/权重；没有证伪真实任务条件结构编译，也不意味着修复后必有收益。旧路线的投入停止仍保留。
- 证据：`ziyu24/cqc_P8@3d0d9a3c689329d9276ea2ba7b1c77ff9da15852` 的 `runs/r003/mission_set_compile.py`、`runs/r004/metrics-long.csv`；源码复审核正见 `ziyu24/cqc_P8@f37b9ebac2b937384cca5b33c50edff28eff9a73` 的 `lab/result.md`、`lab/discussion.md`、`lab/failed_methods.md`。

## 教训二：代理分数不预测尾部风险时不应驱动压缩

- 失败命题：当前采集条件代理能够识别对压缩特别敏感的样本。
- 失败原因：固定前缀截取和降采样再上采样下，短轴比值代理与真实损伤变化相关性方向不符，并未形成所需尾部信号。样本内加入交互项的R²改善不能替代独立场景外预测证据。
- 后续做法：在不参与构造分数的样本上检验排序、尾部召回和校准；预测效度失败时先更换可观测量，不直接进入条件路由或压缩优化。
- 边界：结论否定这一代理及损伤操作下的可用性，不否定采集元数据有信息，也不证明不存在采样—容量交互。不通过追加同类分数挑出成功。
- 证据：`ziyu24/cqc_P8@aa11ff40ee1da87812ce819c65108a2cfb3175d8` 的 `runs/r005/acor_probe.py`、`runs/r005/analysis.yaml`；解释边界见 `ziyu24/cqc_P8@f37b9ebac2b937384cca5b33c50edff28eff9a73` 的 `lab/result.md`。

## 教训三：伪量化与导出验证不能替代真实后端保真验证

- 失败命题：局部伪量化足以判定整机量化价值；或全卷积模拟已恢复、ONNX转换检查通过且引擎具有实际INT8覆盖，就可宣布真实部署恢复。
- 失败原因：早期bbox-head W4A4仅筛选四个全局裁剪倍率，未覆盖骨干/颈部或真实低比特执行；OBB与HBB选同一倍率不能证明专属收益。后续全卷积模拟初始化AP75为85.667、同一静态ONNX CPU参考85.387，但真实95卷积静态W8A8仅6.484。原8图原/静态ONNX输出完全相同，仍未防止实际后端原始输出大幅偏离。构建完成、导出检查与执行精度覆盖分别回答不同问题，不能互相替代。
- 后续做法：分别报告PTQ/QAT、量化范围、粒度、标定与舍入规则。按冻结输入核对模拟、导出参考及实际引擎的原始输出，再核验同后处理指标、实际精度/回退和完整效率；先找首个可复现数值分歧，再设计区分融合或算子因素的有界对照。零参数更新符合PTQ设定，不能把缺少QAT当作反证。
- 边界：局部四候选负结果不否定全模型PTQ。最新证据将该图的大差距缩至后端转换/执行环节，但不证明BN、某内核或NVIDIA缺陷为根因，不否定所有强PTQ。ONNX与模拟AP75差−0.280点、95%区间[−1.605,+0.891]不是等价证明；未确认真实整数精度恢复、部署加速或OBB特有机制。局部子图通过也不能排除生产图对应区域。
- 证据：`ziyu24/cqc_P8@bbe43e3ddc34536edcf04c61b8604ed1a3d86e2b` 的 `runs/r006/quotient_ptq_probe.py`、`runs/r006/analysis.yaml`；最新冻结图、实际后端产物核验和独立CPU参考见 `ziyu24/cqc_P8@c054e6f35d9d274e3ea6911d19cee6c73355c8fe` 的 `lab/result.md`、`lab/failed_methods.md`、`src/r021_static_weights.py`、`src/onnx_cpu_reference_audit.py`。结果页保留报告/图/预测哈希、完整CPU证据入口、配对区间及统计入口修复导致的一次重复推理成本；未重新执行原TensorRT全图前向。

## 教训四：诊断观测会改变被测执行图，必须核验实际精度与输出

- 失败命题：将内部张量标为输出后测得误差很小，就能排除原生产INT8图的对应早期区域。
- 失败原因：捕获点仅是首个卷积块，且捕获图前两卷积实际已经回退Half，两个颈部Resize也改变精度，并有融合/tactic变化；原生产对应卷积仍为Int8。将这种捕获值称作真实INT8 stem证据，既误认观测位置，也忽略观测本身改变了被测图。
- 后续做法：绑定捕获tensor到原生模块和实际引擎层，审查权重/输入/输出类型、融合与算子实现；保留原生产输出做同输入配对，再决定观测是否能外推生产图。构建失败或浮点对照不能代替缺失的真实INT8数值。
- 边界：这反驳当前无扰动观测和stem排除解释，不证明所有中间观测方法不可行，也不证明生产首层确实出错。图文件名和INT8标志不是实际执行精度的充分证据。
- 证据：`ziyu24/cqc_P8@38d5ad6fc39d482cdace51805c8eec4f8bc91150` 的 `src/r011_segment_audit.py`；实际序列化引擎inspector、对应哈希和反序列化重建入口见 `ziyu24/cqc_P8@f37b9ebac2b937384cca5b33c50edff28eff9a73` 的 `lab/result.md`、`doc/r012_execution.md`、`src/r012_compare_existing.py`。

## 教训五：指标或容差裁决不能替代连续误差与机制判断

- 失败命题：相同校准缓存与相同AP可证明重建图数值相同；混合诊断图大幅恢复就证明某层或NMS是根因；或严格容差未过就代表没有改善。
- 失败原因：原图与审计重建缓存逐字节相同、AP也相同，但固定训练图原始输出仍有非零差异。混合图AP75提升约30.407点，同时改变前端、上采样、融合与tactic；固定浮点最终候选ID仅再恢复约3.010点，不能把复合图的整体提升全归于离散选择。后续等值尺度表示使小图相对正确CPU参考的最大NRMSE从8.769降至0.002547，仍超1e-5；据门槛将其写成没有恢复，遗漏了真实且显著的连续误差下降。
- 后续做法：区分缓存身份、逐元素误差、指标复现与统计差异；预先固定比较臂和主要干预，报告恢复与剩余损失的同一分母。单因果归因需要能区分同时改变因素的有效对照，不能事后选最高干预；保留逐图预测及不确定性以便复核。对数值干预同时报告旧/新误差、通过比例与语义保持；未达完整保真不抹去局部改善，改善倍数也不代替任务指标。
- 边界：标准重建在该固定数据/模型上没有恢复，混合图的部分正向恢复真实存在；但强PTQ未实现、单算子因果未识别，旧混合图主要ID干预只有点估计且当时完整逐图预测未保存；后来已有完整预测的特定P00测量仍为AP75=0，不能补出旧混合图的缺失区间，也不能否定所有非前端回退。局部比较结束不等于所有量化失效、所有构建等价或机制效应为零；未分离的恢复线索仍需有界对照，不能仅因主要ID干预较小而清空数值问题。新等值表示的CPU端点保持与小图改善均已验证，但伴随融合变化，仍不识别内部唯一根因或证明全模型修复；辅助ULP未显示额外收益，不据此追加尺度搜索。
- 证据：`ziyu24/cqc_P8@f37b9ebac2b937384cca5b33c50edff28eff9a73` 的 `lab/result.md`、`lab/discussion.md`、`src/r012_compare_existing.py`、`src/r012_contract.py`、`src/r012_inspector_diff.py`；结果页保留原始报告哈希与重建入口。特定P00后续测量及解释边界见 `ziyu24/cqc_P8@c9992616e057071cfdac6b18bd43ab919eeca71c` 的 `lab/result.md`。等值表示对照及解释核正见 `ziyu24/cqc_P8@5dd67246dec926527c4a5d4f7b06347a25925a50` 的 `src/r025_scale_alias.py`、`lab/result.md`、`lab/failed_methods.md`；结果页绑定原始报告、两项plan和张量包哈希。

## 教训六：替换尺度后的恒等通过不能验证原变换的数值实现

- 失败命题：连续范围平衡尺度未达恒等容差后，舍入为2的整数次幂得到零误差，就可宣布原初始化的数值前置通过，并把原失败归于普通FP32舍入累积。
- 失败原因：固定模型与相同训练图范围下，原连续平方根尺度的全网最大NRMSE约0.015649，二进制尺度为0。舍入改变尺度值和精确范围平衡，即使不使用验证数据，也已经改变被验证方案。原入口未记录或禁用卷积TF32，只有float32张量类型及孤立单层检查，不能识别完整网络误差来自普通FP32舍入还是其他后端数值行为。
- 后续做法：冻结原尺度、原输入和容差，记录实际后端精度许可；比较同模型重复底线及仅改变精度许可的对照。原方案与辅助表示分别报告，不能用辅助臂成功替代失败主臂；保留失败尺度、逐输出误差和实现身份。
- 边界：二进制尺度的已测零误差有效，但不是完整强PTQ、量化收益或零部署开销。后续在固定模型/尺度/8图及不变1e-5容差下，关闭TF32时连续最大NRMSE为1.955525e-6并通过，允许卷积TF32时为0.015649并失败；两模式原模型重复误差均0，全部576个输出数组已独立复算。故不能否定连续迁移在禁用TF32时的数值正确性。该对照证明许可设置影响此网络的恒等误差，仍不证明旧进程逐层核选择，更不能解释标准INT8失效。
- 证据：`ziyu24/cqc_P8@c9992616e057071cfdac6b18bd43ab919eeca71c` 的 `lab/result.md`、`lab/failed_methods.md`、`src/r016_migration_contract.py`、`src/r017_precision_identity.py`；原连续实现为 `ziyu24/cqc_P8@346e40bfc52c52b1bfeaa3485952125ac9d62474` 的 `src/r016_migration_contract.py`，二进制修改为 `ziyu24/cqc_P8@1cfc50566cd8306e2105be7fca1eed0356d779b9` 的同路径。结果页保留两套报告、尺度和恒等文件哈希；后续实测及全部原始张量的独立复核见 `ziyu24/cqc_P8@aae85883ba62334488fefb95d198656ea9c65c43` 的 `lab/result.md`、`lab/discussion.md`；报告与张量包哈希均在结果页登记。

## 教训七：局部重构目标下降不能替代对廉价初始化的检测收益比较

- 失败命题：逐层训练校准重构MSE进一步下降，足以宣布尺度搜索比连续范围平衡初始化更有检测价值。
- 失败原因：固定单模型、训练校准集与有限候选搜索下，87个唯一卷积中86个采样MSE下降、没有上升，但181图AP75的搜索减初始化为−0.651点，配对95%区间[−2.610,+1.338]。相对于无迁移的巨大恢复已由初始化实现，不能把共同恢复全部记为搜索贡献。
- 后续做法：预先保留无迁移、廉价初始化与完整搜索对照，分别报告局部目标和下游指标，冻结训练图选择后再做同图配对评测；没有增量证据时不按验证集追加搜索至成功。模拟恢复应进一步核实真实后端数值和执行精度。
- 边界：本次有限采样搜索没有证明增量，不等于搜索总体效应为零、MSE必然有害或完整作者算法无效。初始化AP75=85.667、原生=86.050的有条件正结果必须保留；区间跨0不是等价证明，尚无真实整数恢复、部署加速或OBB特有机制结论。
- 证据：`ziyu24/cqc_P8@78c3bc8ee97f81c2a84a9ce05bcbac61d13bb773` 的 `lab/result.md`、`lab/failed_methods.md`、`src/r018_migration_ptq.py`、`src/r018_quant_contract.py`；结果页保留完整搜索、原始预测及bootstrap哈希和重建入口。原登记已独立核验图级预测完整性、校准文件和区间分位数；最新CPU参考审计还对已保存初始化、原生和TensorRT预测复算官方匹配/AP，但未重跑搜索臂前向或将其升级为真实整数部署。

## 方法族停止索引

| 方法族 | 最强负证据或限制 | 停止范围 | 当前 main 证据路径 |
| --- | --- | --- | --- |
| 哈希选择的固定结构 | 名义编译未实现，局部AP较差，端到端延迟未测 | 停止旧实现投入；撤回完整效率与大方向否决 | `lab/result.md`；`lab/failed_methods.md` |
| ACOR | 负相关且尾部筛查不通过 | 停止当前代理驱动压缩 | `lab/result.md` |
| bbox-head W4A4 | 四倍率局部fake quant，OBB相对HBB无恢复 | 停止当前筛查及整机外推；零训练不是PTQ缺陷 | `lab/result.md`；`lab/failed_methods.md` |
| 标准真实W8A8及审计重建 | AP75均0；首坏层未知 | 当前链路不可用；不否定强PTQ | `lab/result.md` |
| 内部捕获与混合图 | 捕获改变精度，部分恢复且主要ID干预恢复有限 | 未分离的恢复线索仍需有效对照；不作单层/NMS根因或部署收益声明 | `lab/result.md`；`lab/discussion.md` |
| 连续尺度迁移的数值恒等 | 固定连续尺度禁用TF32时通过，允许时失败 | 不停止正确精度设置下的原变换；辅助表示不替代主臂，不外推INT8根因 | `lab/result.md`；`lab/failed_methods.md` |
| 静态显式QDQ实际执行 | 模拟/CPU ONNX约85分、实际INT8 AP75仅6.484；等值表示使小图误差大幅改善但仍超严格容差 | 不追加训练或尺度搜索；保留局部改善及工程收尾价值，全图修复未验证，不宣称特定根因或否定所有PTQ | `lab/result.md`；`lab/failed_methods.md` |
| 连续初始化后的有限尺度搜索 | 局部MSE改善而AP75增量未获支持；初始化有明显恢复 | 停止追加同类搜索，保留初始化与固定后端验证；不否定所有搜索 | `lab/result.md`；`lab/failed_methods.md` |
