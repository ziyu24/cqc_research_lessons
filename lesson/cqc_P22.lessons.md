# cqc_P22：稀疏带噪弱监督旋转检测的基线可判别性与及时反馈

## 快速阅读路径

先读来源项目`README.md`、`lab/result.md`及`lab/failed_methods.md`，再读`lab/discussion.md`中的复核和限制。原始代码核对在`doc/SPWOOD-code-audit.md`。本页不替代原始指标、运行记录及具体方法实现。

## 项目研究什么

少量图像中的部分实例带人工Point或HBox，保留标签还可能位置、几何或类别错误；其余图像完全无标签，目标是学习已知类别OBB。图内额外漏标是与不额外漏标的Noisy-PWOOD参考之间的差异。当前实验仅研究合成几何噪声，没有覆盖类别错误。

## 领域位置与当前结论

HBox在单种子、HRSC、训练前缀下显示明显剩余噪声损失，但稀疏对噪声损失的交互依评价IoU而异。Point旧实现四个条件均无检测信号；修正非方图坐标网格后，干净全保留条件已恢复teacher AP50=13.89%、student=19.54%，但teacher AP75仅0.38%。后续稀疏Point重测的干净/噪声teacher AP50为3.0303%/0，两者AP75均零；干净组又有额外恢复，故差值不能全归因于噪声，低绝对精度也限制鲁棒性判别。冻结GT替换仅在全保留Point把主要AP50残差定位到尺寸；稀疏干净/噪声只换尺寸AP50为12.20%/1.30%，整框替换20.75%/34.16%，尺寸解释不再充分。这些不是训练方法成绩或因果证明。尚无新方法成功或首创证明；PWOOD原文本身已有几何噪声实验，不能以任务名称不同认定整个领域空白。

进一步独立重训、关闭无标签框回归后，teacher末点AP50/AP75达到29.35%/4.94%；但开关启用前AP50已比参考高7.99个百分点。因此性能改善真实，全部涨幅归因于该项的解释不成立；同种子不等于同一个干预起点。

共同状态尾程已完成：关闭/开启的teacher AP50为32.39%/41.65%，AP75为3.94%/4.55%，没有支持关闭更好。但关闭臂单独经历中断恢复，历史RNG/loader没有完整复原，故负差也不能被当作开关净因果效应。

分类监督冲突干预已完成干净稀疏状态的共同起点尾程：排除跨监督类型矛盾负项后，teacher AP50为9.0909%，原生为3.7879%，AP75均零，满足预定局部判据。两臂均连续完成6400步，没有额外训练重启；但teacher中心覆盖仅120/541对118/541，均低于起点150/541。这是局部损失干预的积极结果，不证明候选发现全面恢复或严格定位已解决。带噪Point的对应尾程现已完成，两个臂的student/teacher AP50及AP75均零，未满足预定局部判据；teacher AP25为12.7869%对13.1903%，中心覆盖368/541对380/541。机制确实执行且输出不空，但带噪迁移未获支持；性能地板也不足以证明一般机制无效。原范围内带噪HBox的同机制对照尚无训练结果。

## 实际采用过的方法

以公开SPWOOD代码为基线并补齐缺失损失注册，固定HRSC单类划分为训练436图、验证181图、测试453图且不使用测试标签；固定种子42。87张有标图内含282实例，其余349张无标签；Overall稀疏条件保留56实例。Point/HBox、实例保留20%/100%、干净/固定几何扰动形成八个条件，分别训练32000步，实际双卡、全局源batch4和AdamW学习率2.5e-5。最终指标固定为32000步teacher AP50/AP75，另保留五个固定步点；不按最佳时刻改写原终点比较。

100%仅指有标图像内全实例保留，仍是部分弱监督参考，不是全图像标注、完整OBB强监督或数学上界。未将其他项目不同实例预算和框架的AP作为本项目直接对照。

后续先用完整冻结验证预测排除全空输出、非有限几何等解释，再以合成非方图种子/前景/梯度反例核对Point分水岭坐标。仅修正网格后从相同初始化重训一个干净全保留条件，保留原32k日程并真实启用首个曝光等效epoch及五个共同点的即时评测。随后固定六份Point/HBox、student/teacher预测，分别替换中心、长边、短边、角度、两边和整框；仅诊断副本读取GT，预测分数、数量、顺序和固定关联不变，不回灌训练。

之后通过实际生效的原生分项权重，仅清零无标签bbox回归，保留无标签分类、centerness及全部弱标签损失，从同初始化和种子再训练一个32k条件。全局unsup_weight在该公开代码中并未乘入返回损失，不能用作开关。日志确认384条无标签阶段记录中回归为0、其他两项活动，末点与合法AP50-best相同；同时检查了干预前曲线，发现明显轨迹差异。

之后补做修复后的20%实例Point干净/噪声两个原生损失条件，同为32k和单种子。包括首个曝光等效epoch的六个固定点均完成评测，87张弱标注图中的49张空图仍保留，56点中18点有噪声。干净组经历两次额外恢复，噪声组未同样重启；最终两分支均为干净组较高，但绝对AP50很低，不能把这种观察升级为噪声净作用或新方法成功。

进一步从受保护的干净稀疏32k完整状态分叉两个6400步尾程，仅排除已标注正例在另一监督类型中同位置、同类别的矛盾分类负项，与保留原生损失的对照比较。两臂使用相同首增强批次、seed42、两卡及原生训练语义，终点固定38400。原生损失梯度反例和各rank实际移除统计均核验；没有借助隐藏真值判错或增加标注。绝对指标和候选中心覆盖同时报告，不将AP增益等同于总体召回改善。

同样的两个6400步尾程随后应用于受保护的稀疏带噪Point状态；56点中的18个几何扰动、49张空图及其余训练语义保持不变。排除臂两rank实际移除247376/240362个矛盾负项，原生臂保留全部冲突，两臂均连续完成，没有训练重启。固定验证全集、完整预测身份、实际双rank更新和模型实体均已核验；相同首增强批次仍不保证全程随机轨迹逐位相同。

## 教训一：关键基线没有学习信号时，重复零差异不能回答鲁棒性问题

- 失败命题：只要完成干净/带噪及稀疏/全保留的矩阵，即使基线始终零AP，也能凭噪声差为零判断鲁棒性；保存早期检查点、训练完成后再评测足以避免无效重复。
- 失败原因：Point四条件在五个固定步点的student和teacher AP50/AP75均为零，连干净全保留条件也没有可判别性能。执行代码先跑完一个32000步训练臂，才评测其全部已存检查点；缺少关键对照持续零分后终止依赖臂的分支，因而保存了模型也没有形成及时性能反馈。全轮222.19 GPU小时包含HBox、Point与失败恢复，不能全部归因Point，也不能从事后结果精确推定可节省时间或首次必败步数。
- 后续做法：先完成可判别关键条件及必要阳性对照，检查点生成后及时评测，再决定是否开展依赖它的重复条件。首个曝光等效epoch可用于输入、预测几何、分配和梯度诊断；预先声明重复零信号的任务止损条件，并保留完整合法验证分母。停止本次验证或依赖臂，与否定整个范式分开报告。
- 边界：首轮零AP可能出现在warmup或teacher开始EMA以前，不能单点判死。旧网格错配图像轴及像素端点，合成反例和一个同条件修复训练恢复了信号，故“当前Point全部零分”已过时；不证明唯一根因或所有条件已修复。即时双卡评测已在驻留训练中运行并返回原循环，但两个判定点均有非零AP，没有实际触发GPU零信号停止分支。该教训不证明Point监督不可行。
- 证据：`ziyu24/cqc_P22@6bf6aaa20358b588295b9652fb5f394620fe3c1e`，`lab/result.md`、`lab/failed_methods.md`、`lab/discussion.md`、`src/check_voronoi_runtime.py`、`src/train_spwood.py`、`src/early_eval_hook.py`及`doc/SPWOOD-code-audit.md`；[来源结果与修正](https://github.com/ziyu24/cqc_P22/blob/6bf6aaa20358b588295b9652fb5f394620fe3c1e/lab/result.md)。已核来源全部远端分支，仅main；对照原始统计及执行记录，只读复核没有新增训练。

## 教训二：宽松定位下的噪声放大不能代替跨定位阈值一致结论

- 失败命题：稀疏条件下噪声造成更大的AP50下降，就能推出几何噪声在所有检测精度指标上都被稀疏一致放大。
- 失败原因：HBox的20%/100%保留条件下，干净到噪声的AP50分别为36.7→19.3及88.7→86.5，绝对下降17.4/2.2个百分点，交互+15.2；AP75则为9.1→5.5及42.4→28.9，下降3.6/13.5个百分点，交互-9.9。两个IoU标准的实际绝对差方向不同，不能只报告有利阈值。稀疏干净AP75本身较低是解释时的范围限制，尚未证明它就是反向交互的原因。
- 后续做法：报告全部绝对AP、干净基线水平、各条件噪声损失及预定交互；不要把相对下降与绝对百分点交互混用。可以保留AP50的局部正向研究信号，同时诚实记录AP75反向及Point不可判别，区分任务困难、方法有效和一般机制主张。
- 边界：单种子、单类HRSC、固定合成噪声及32000步前缀，不证明一般统计显著性、跨种子稳定或完整日程结论。不一致交互不自动取消稀疏带噪监督范式的研究价值，100%保留参考也不是理论性能上界。
- 证据：`ziyu24/cqc_P22@3909a26d412330d1da601e4e177b54043f7f1be0`，`lab/result.md`、`lab/discussion.md`、`configs/research.json`、`src/summarize.py`；[来源结果](https://github.com/ziyu24/cqc_P22/blob/3909a26d412330d1da601e4e177b54043f7f1be0/lab/result.md)。原始八臂最终指标与交互已回读一致，来源仅main。

## 教训三：真值几何替换的解释受候选与标签预算限制，不能证明训练因果或方法收益

- 失败命题：固定预测经GT尺寸替换后AP接近强参考，足以证明无标签伪框回归是原始失败原因，或者代表一种可部署的尺寸修正收益，并能直接外推到稀疏标签条件。
- 失败原因：同一181图/541GT下，Point末点teacher AP50从13.89%经两边替换到84.01%，只换角度为13.91%，支持尺寸是主要AP50残差；但12.8k无标签分支尚未启用时，尺寸替换已使AP50从9.98%升到71.62%，尺寸错误先于该分支存在。末点尺寸替换AP75仍仅30.40%，整框替换87.35%，不能连严格IoU下的中心/角度作用也排除。替换依赖GT和固定关联，不是学得的修正或全局优化上界。
- 稀疏反例：修复后的稀疏干净teacher从3.0303%经尺寸替换到12.2010%、整框替换到20.7537%；噪声从0到1.2987%/34.1593%。干净候选中心只覆盖150/541 GT，噪声覆盖355/541但尺寸和角度更差。原始AP均精确复现，说明全保留场景的尺寸解释不能直接迁移；整框替换仍保留关联、候选、重复与排序约束，低分不能唯一诊断分类失败，噪声的整框结果更高也不是噪声有益的证据。
- 后续做法：冻结合法验证全集、分数、数量、顺序和关联，先复现原始指标再做单几何量替换；同时报告多个IoU。用启用时序检验“先因后果”，再以保持其他训练语义的单变量损失对照检验是否强化已有偏差；不得把GT比例直接用作训练或部署规则。
- 边界：仅限固定单种子、冻结预测和既定中心邻域关联；能限制因果叙述，不能唯一定位伪框、目标构造或损失梯度中的根因。后续回归项消融已有更高末点分数，但存在干预前轨迹差异，仍需区分诊断对照、新方法及原稀疏带噪目标，详见第四条。
- 证据：`ziyu24/cqc_P22@a98dae0b4dae650bc4a0790e538df6e53dda409a`，`lab/result.md`、`lab/discussion.md`、`src/geometry_attribution.py`、`src/r004_execute.py`及`src/point_diagnostics.py`；[来源归因及边界](https://github.com/ziyu24/cqc_P22/blob/a98dae0b4dae650bc4a0790e538df6e53dda409a/lab/result.md)。双rank冻结统计、原始AP复现及burn-in时序已核对；追加稀疏四份预测、来源散列和完整比较已只读回查，无新增训练。全部来源远端分支仅main，未把条件替换当真实模型性能。

## 教训四：相同种子或共同起点不能替代全程恢复历史的配对核验

- 失败命题：初始权重、配置和seed一致，只改后期损失开关，就能将两个独立重训的全部末点差归因于这个开关。
- 失败原因：关闭无标签框回归的32k末点teacher AP50/AP75比参考高15.4596/4.5629个百分点，但12800步尚未启用干预时，teacher AP50已高7.9894个百分点，student已高11.6092个百分点。干预起点并不相同，随机流或非确定性来源尚未唯一定位。直接减去前期AP差也不能在线性关系未经验证时构成因果净收益。
- 后续反例：改从相同完整状态及相同首批开始后，关闭臂额外从保存的32109步恢复，开启臂没有同样重启。最后teacher关闭减开启为-9.2658/-0.6091个百分点，而student方向相反。初始首批一致并未保证后续随机流一直受控；重启缺少完整RNG/loader恢复，不能把未达到预定主判据升级为损失局部命题被严格反证。
- 后续做法：先回读干预前曲线与实际损失路径；需要隔离后期作用时，从不可变的同一完整模型、teacher和优化器状态分叉，用相同采样规则、共同末点及选优规则比较。核查实际首批及恢复计数；若没有历史RNG/loader状态，就明确共同重启采样与历史逐位重放的区别。保留真实的更高性能，而不把它冒充已分离的机制效应。
- 边界：这限制的是当前设计的强归因，不证明关闭回归在所有条件无效，也不否定Noisy-SPWOOD。共同状态尾程的指标保留，但必须披露非对称恢复；即使无该混杂，也不能追溯等同原burn-in时刻干预或证明跨种子稳定性。停止把关闭项作为已获支持的改进，不为追求有利结论自动追加重复训练。
- 证据：`ziyu24/cqc_P22@9d251df2ef3d9079604b8d06e3ce2b785a847a6e`，`lab/result.md`、`lab/discussion.md`、`lab/failed_methods.md`、`src/unsup_bbox_ablation.py`及`src/train_spwood.py`；[原始曲线和恢复复核](https://github.com/ziyu24/cqc_P22/blob/9d251df2ef3d9079604b8d06e3ce2b785a847a6e/lab/result.md)。六点配对验证全集、预测散列、loss实际开关、四份best/final实体及非对称恢复已回读；全部来源远端分支仅main，复核没有新增训练。

## 教训五：较充分标注下恢复信号，不等于稀疏协议已具备鲁棒性判别力

- 失败命题：全保留弱标签下修复了检测信号，或稀疏条件出现微小非零AP，就足以认为稀疏基线已可用于强噪声效应判断。
- 失败原因：相同32k前缀下，网格修复后的稀疏Point干净teacher AP50/AP75仅3.0303%/0，噪声为0/0；student为4.5455%/0对0/0。无标签分支损失已正常启用，但干净性能仍接近地板。两个预定停止点因微小非零AP没有触发严格零止损，只证明停止条件未满足，不证明研究基线足够强；干净组额外恢复又限制了净噪声归因。
- 后续做法：先在目标实例预算下核对绝对性能和完整曲线，区分工程可运行、非零信号及科学比较可判别性。旧全保留诊断不能直接外推稀疏条件；优先复用已冻结预测检查几何可恢复误差、候选覆盖及固定排序限制，再据证据安排必要干预，不用微小非零作为追加长训练的理由，也不事后修改旧停止判据。
- 边界：仅限HRSC单类、单种子、固定合成几何噪声、当前公开实现适配及训练前缀，不能证明更长训练或整个Point范式不可行；HBox的已有噪声损失证据仍有效。稀疏几何诊断限制了仅尺寸解释，详见第三条。后续去除分类矛盾负项在干净共同状态尾程取得teacher AP50的+5.3030个百分点，纠正了“干预尚无检测结果”的旧状态；AP75仍零、中心覆盖只多2/541，说明窄范围改进不能自动升级为稀疏鲁棒基线可用。带噪对应尾程已实际移除冲突，却仍双臂AP50/AP75为零，故本次迁移未获支持，不能把执行正确或零差包装成鲁棒性。此负结果不否定干净积极结果，也不能从性能地板唯一判定分类机制或Point范式无效；不追加训练追求有利时间点。
- 证据：`ziyu24/cqc_P22@3ab0e505554cef4fbb6bb237e5eef4221fb7d481`，`lab/result.md`、`lab/failed_methods.md`、`lab/discussion.md`、`src/r007_execute.py`和`src/early_eval_policy.py`；[目标稀疏协议结果与限制](https://github.com/ziyu24/cqc_P22/blob/3ab0e505554cef4fbb6bb237e5eef4221fb7d481/lab/result.md)。12份验证报告、预测摘要、四份模型及恢复记录已只读核对；来源全部远端分支仅main，复核没有新增训练。
- 追加证据：`ziyu24/cqc_P22@b7ab0b08b6b65d061468ce051e21fc7603f1a1bd`，`lab/result.md`、`lab/discussion.md`、`src/classification_conflict.py`及`src/train_spwood.py`；[干净分类冲突干预与覆盖边界](https://github.com/ziyu24/cqc_P22/blob/b7ab0b08b6b65d061468ce051e21fc7603f1a1bd/lab/result.md)。四份原始评测、八份预测身份摘要、四份保护模型及连续双rank计算记录已只读核验；全部来源远端分支仅main。该次复核未新增训练。
- 带噪追加证据：`ziyu24/cqc_P22@9b3ef4c82295b2dd7261fbb8b266324532f33dd8`，`lab/result.md`、`lab/failed_methods.md`、`lab/discussion.md`和`src/classification_conflict.py`；[带噪迁移与零性能边界](https://github.com/ziyu24/cqc_P22/blob/9b3ef4c82295b2dd7261fbb8b266324532f33dd8/lab/result.md)。四份固定点评测、八份完整预测身份、四份保护模型、各rank冲突统计与连续计算记录已只读核验；来源全部远端分支仅main。复核未新增训练，HBox对照仅交付，不登记为成功或失败。

## 方法族停止索引

没有整个Point监督、HBox监督或Noisy-SPWOOD方法族获授权停止。全保留Point修复恢复了检测信号，但修复后的稀疏干净/噪声基线已实测且接近性能地板；HBox仍保留局部积极证据。全局关闭回归未获支持，既有轨迹差与非对称恢复限制净因果解释。项目继续推进；稀疏冻结几何诊断已完成，单纯尺寸解释不足；分类冲突负项排除已获得干净状态的局部AP50收益，但严格定位和总体覆盖没有解决，带噪Point对应尾程未获支持，不再自动追加该尾程。带噪HBox同机制对照尚无结果。即时评测已执行，中间点并发容量仍有失败，真实GPU严格零停止分支尚未触发。
