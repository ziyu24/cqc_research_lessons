# cqc_T1 科学问题与失败教训

审计基线：`ziyu24/cqc_T1@ffda4284e0183849d5287f931950aa5dad97ba66`

## 项目研究什么

项目研究大幅遥感图像切片会不会使同一目标的旋转检测结果随网格原点变化，以及能否通过对齐、融合、局部修复或精确流式计算降低这种分区不稳定性。

## 实际采用过的方法

项目测量不同切片相位下的实例状态翻转，测试随机偏移、对齐、合并、融合和提案稳定性；又以整幅母图和冻结特征替换作诊断，并尝试保持完整检测器输出等价的流式执行。

## 教训一：总体精度不能替代实例级分区稳定性

- 失败命题：切片方案的总体 AP 相近，就说明单个目标对网格原点稳定。
- 失败原因：聚合指标会抵消不同实例的得失，无法观察同一实例随相位变化的检出、类别和角度翻转。
- 后续做法：建立母图实例身份，对同一对象做跨相位配对，并同时报告总体性能和实例稳定性。
- 边界：实例稳定性是额外估计对象，不自动等于更高 AP 或更好的下游效用。
- 证据：`ziyu24/cqc_T1@ffda4284e0183849d5287f931950aa5dad97ba66` 的 `reports/partition_instability_top_journal_route.md`、`reports/dota10_evaluation_closure.md`。

## 教训二：低翻转率可能来自提案坍塌而非鲁棒性

- 失败命题：某方法产生更少的状态翻转，因此检测更稳定。
- 失败原因：提案召回同时下降时，模型可能稳定地漏掉目标；稳定性指标脱离任务能力会奖励退化表示。
- 后续做法：把稳定性与召回、精度和覆盖作为合取条件，并设置保持任务能力的基线。
- 边界：在任务能力匹配后，较低翻转率才可解释为稳定性改善。
- 证据：`ziyu24/cqc_T1@ffda4284e0183849d5287f931950aa5dad97ba66` 的 `reports/partition_instability_top_journal_route.md`、`reports/dota10_spear_stage_a.md`。

## 教训三：整图参考证明可避免切片损伤，但不是部署方案

- 失败命题：整幅母图推理表现更好，便足以证明已经找到可部署的分区修复方法。
- 失败原因：整图参考消除了切片，但可能超出显存和延迟预算；它只给出方向性上界，没有提供满足资源约束的算法。
- 后续做法：把整图作为正参考或 oracle，另行要求候选在显存、延迟和输出质量上满足部署条件。
- 边界：若目标硬件确实能整图推理，参考本身可以成为合法基线。
- 证据：`ziyu24/cqc_T1@ffda4284e0183849d5287f931950aa5dad97ba66` 的 `reports/partition_instability_top_journal_route.md`、`reports/dota10_partition_free_reference.md`。

## 教训四：冻结特征修复证明局部充分性，不证明可学习性

- 失败命题：把失败样本的局部特征替换后可以恢复检测，就证明训练一个局部修复模块会成功。
- 失败原因：冻结替换是事后干预，使用了理想参照特征；实际训练还需从可用输入识别何时、怎样生成该特征，项目训练结果没有实现同样恢复。
- 后续做法：将因果充分性、可预测性和端到端可学习性分成三个实验，不能从第一个直接跳到第三个。
- 边界：干预结果仍能定位潜在瓶颈并否定“局部特征无关”，但不是方法有效性证据。
- 证据：`ziyu24/cqc_T1@ffda4284e0183849d5287f931950aa5dad97ba66` 的 `reports/dota10_cpet_gate1.md`、`reports/dota10_cpet_formal_result.md`、`reports/dota10_cpet_phase_gate.md`。

## 教训五：精确流式计算必须同时满足输出、内存和延迟

- 失败命题：局部算子单测等价且显存下降，就构成完整检测器的精确流式方案。
- 失败原因：全局注意力需要同步屏障，区域 FPN 和底层算子存在形状相关浮点差异；即使内存下降，运行时间也可能不可接受。
- 后续做法：在完整模型和真实输入上同时验证逐输出等价、峰值显存和端到端延迟，并把全局算子单独建模。
- 边界：允许近似输出或离线高延迟的场景可放宽条件，但必须更改“精确且实用”的主张。
- 证据：`ziyu24/cqc_T1@ffda4284e0183849d5287f931950aa5dad97ba66` 的 `reports/partition_instability_top_journal_route.md`、`reports/dota10_pnee_p0b2_closure.md`。
