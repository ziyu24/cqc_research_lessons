# cqc_research_lessons 规则

- 默认使用中文，结论优先；事实、推测和未知分开。
- 本库保存可跨任务复用的科学失败教训，不保存内部任务号、门槛流水账或运行状态。工程故障、资产缺失只能作为证据边界，不能冒充科学负结果。
- 每个有实质科研证据的仓库只对应根目录文件 `<仓库名>.lessons.md`，不得再建立项目子目录、卡片体系、审批、认领、多角色状态或多层回执。
- 写入前必须逐项目阅读已提交的研究问题、方法、结果和失败记录；不能只做关键词摘抄。每个文件先说明项目研究什么、实际采用过哪些方法，再归纳中等粒度教训。
- 每条教训固定写失败命题、失败原因、后续做法、边界、来源提交和相对证据路径。标题和正文必须脱离 `rNNN` 等项目内部编号也能被本领域研究者理解。
- 修改前先读当前项目文件和 `INDEX.md`。新证据推翻旧条目时直接修正，并说明旧结论失效的科学原因。
- 不提交秘密、连接信息、绝对路径、完整日志、数据、权重或未公开稿件。
- 完成前运行 `python tools/validate.py .` 与 `python -m unittest -v tests.test_validate`，提交并推送，再回读远端提交与文档。
