# cqc_research_lessons 规则

- 默认使用中文，结论优先；事实、推测和未知分开。
- 只保存已由来源项目证据支持的避坑点。不得把想法、聊天结论、资产缺失、工程失败冒充科学证伪。
- 项目名必须是根级目录，条目只写入 `<仓库名>/lessons.md`；不要恢复分类卡片、审批、认领、
  多角色状态或多层回执。
- 条目保持中等粒度：同一根因的重复现象合并，不同根因分开；每条都写类型、失败原因、避坑、
  适用边界、来源提交和相对证据路径。
- 修改前先读当前项目文件和 `INDEX.md`。若新证据推翻旧条目，修正条目并明确旧结论为何失效，
  不把错误历史继续传播。
- 不提交秘密、连接信息、绝对路径、完整日志、数据、权重或未公开稿件。
- 完成前运行 `python tools/validate.py .` 与 `python -m unittest -v tests.test_validate`，提交并推送，
  再回读远端提交与文档。
